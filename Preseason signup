<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="robots" content="noindex, nofollow">
<title>Lions Preseason Sign-Up</title>
<style>
  :root {
    --navy: #0D1B3E;
    --navy-2: #13234f;
    --red: #C8202A;
    --gold: #E6B43C;
    --ink: #EDEFF5;
    --ink-dim: #A9B0C6;
    --card: #16224A;
    --line: rgba(230,180,60,0.25);
  }
  * { box-sizing: border-box; }
  html, body {
    margin: 0; padding: 0;
    background: var(--navy);
    color: var(--ink);
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    -webkit-font-smoothing: antialiased;
  }
  .steps {
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 300px;
    z-index: 0;
    pointer-events: none;
  }
  .wrap {
    position: relative;
    z-index: 1;
    max-width: 560px;
    margin: 0 auto;
    padding: 48px 20px 80px;
  }
  .eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.18em;
    font-size: 12px;
    color: var(--gold);
    font-weight: 700;
    text-align: center;
    margin-bottom: 10px;
  }
  h1 {
    font-family: "Arial Black", Impact, "Helvetica Neue", sans-serif;
    font-weight: 900;
    font-size: clamp(38px, 9vw, 56px);
    line-height: 0.98;
    text-align: center;
    letter-spacing: -0.01em;
    margin: 0 0 6px;
    text-transform: uppercase;
  }
  h1 span { color: var(--red); }
  .sub {
    text-align: center;
    color: var(--ink-dim);
    font-size: 16px;
    line-height: 1.5;
    max-width: 420px;
    margin: 0 auto 36px;
  }
  .divider {
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--red), transparent);
    margin: 0 0 32px;
  }
  .info-row {
    display: flex;
    gap: 10px;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 40px;
  }
  .chip {
    background: var(--card);
    border: 1px solid var(--line);
    color: var(--ink);
    font-size: 13px;
    padding: 8px 14px;
    border-radius: 999px;
    display: flex;
    align-items: center;
    gap: 6px;
  }
  .chip b { color: var(--gold); }
  .time-note {
    text-align: center;
    font-size: 12px;
    color: var(--ink-dim);
    max-width: 400px;
    margin: -8px auto 40px;
    line-height: 1.5;
  }

  form { display: flex; flex-direction: column; gap: 22px; }
  fieldset {
    border: none;
    padding: 0;
    margin: 0;
  }
  label.field-label {
    display: block;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 10px;
  }
  input[type="text"], input[type="tel"], select {
    width: 100%;
    background: var(--card);
    border: 1px solid var(--line);
    border-radius: 10px;
    padding: 14px 16px;
    font-size: 16px;
    color: var(--ink);
    outline: none;
    transition: border-color 0.15s ease;
    appearance: none;
    -webkit-appearance: none;
  }
  input[type="text"]:focus, input[type="tel"]:focus, select:focus {
    border-color: var(--red);
  }
  input::placeholder { color: var(--ink-dim); }

  select {
    background-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='14' height='14' viewBox='0 0 24 24' fill='none' stroke='%23E6B43C' stroke-width='3'><polyline points='6 9 12 15 18 9'/></svg>");
    background-repeat: no-repeat;
    background-position: right 16px center;
    padding-right: 42px;
  }

  .radio-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }
  .radio-grid.status { grid-template-columns: 1fr; }
  .radio-card {
    position: relative;
    border: 1px solid var(--line);
    background: var(--card);
    border-radius: 10px;
    padding: 14px 16px;
    cursor: pointer;
    transition: border-color 0.15s ease, background 0.15s ease;
  }
  .radio-card input {
    position: absolute;
    opacity: 0;
    inset: 0;
    cursor: pointer;
  }
  .radio-card .title { font-weight: 700; font-size: 15px; }
  .radio-card .desc { font-size: 12.5px; color: var(--ink-dim); margin-top: 2px; }
  .radio-card:has(input:checked) {
    border-color: var(--red);
    background: linear-gradient(135deg, rgba(200,32,42,0.18), var(--card));
  }
  .radio-card:has(input:checked)::after {
    content: "";
    position: absolute;
    top: 12px; right: 12px;
    width: 10px; height: 10px;
    border-radius: 50%;
    background: var(--red);
  }

  .commit-toggle {
    display: flex;
    align-items: center;
    gap: 12px;
    border: 1px solid var(--line);
    background: var(--card);
    border-radius: 10px;
    padding: 14px 16px;
    cursor: pointer;
  }
  .commit-toggle input { width: 20px; height: 20px; accent-color: var(--red); flex-shrink: 0; }
  .commit-toggle .title { font-weight: 700; font-size: 15px; }
  .commit-toggle .desc { font-size: 12.5px; color: var(--ink-dim); margin-top: 2px; }

  textarea {
    width: 100%;
    background: var(--card);
    border: 1px solid var(--line);
    border-radius: 10px;
    padding: 14px 16px;
    font-size: 15px;
    color: var(--ink);
    outline: none;
    font-family: inherit;
    resize: vertical;
    min-height: 70px;
  }
  textarea:focus { border-color: var(--red); }
  .field-hint {
    font-size: 12px;
    color: var(--ink-dim);
    margin-top: 6px;
    line-height: 1.4;
  }

  button.submit {
    margin-top: 8px;
    background: var(--red);
    color: white;
    border: none;
    border-radius: 10px;
    padding: 16px;
    font-size: 16px;
    font-weight: 800;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    cursor: pointer;
    transition: transform 0.1s ease, background 0.15s ease;
  }
  button.submit:active { transform: scale(0.98); }
  button.submit:disabled { opacity: 0.6; cursor: default; }

  .footer-note {
    text-align: center;
    font-size: 12px;
    color: var(--ink-dim);
    margin-top: 18px;
    line-height: 1.6;
  }

  .success {
    display: none;
    text-align: center;
    padding: 40px 20px;
  }
  .success.show { display: block; }
  .success .mark {
    width: 64px; height: 64px;
    border-radius: 50%;
    background: var(--red);
    display: flex; align-items: center; justify-content: center;
    margin: 0 auto 20px;
  }
  .success h2 {
    font-family: "Arial Black", Impact, sans-serif;
    text-transform: uppercase;
    font-size: 26px;
    margin: 0 0 8px;
  }
  .success p { color: var(--ink-dim); font-size: 15px; line-height: 1.6; }
  form.hide { display: none; }

  .error-msg {
    display: none;
    color: var(--red);
    font-size: 13px;
    text-align: center;
    margin-top: -6px;
  }
  .error-msg.show { display: block; }
</style>
</head>
<body>

<svg class="steps" preserveAspectRatio="xMidYMin slice" viewBox="0 0 400 300">
  <polyline points="-20,300 -20,230 50,230 50,175 120,175 120,130 200,130 200,90 290,90 290,0"
    fill="none" stroke="#E6B43C" stroke-width="1.5" opacity="0.25"/>
</svg>

<div class="wrap">
  <div class="eyebrow">St-Lawrence Lions &bull; D2 RSEQ</div>
  <h1>Preseason<br><span>Starts Here</span></h1>
  <p class="sub">Cardio &amp; conditioning with Coach Ash. Open to returning vets, current core, and anyone ready to earn a spot on this roster.</p>

  <div class="divider"></div>

  <div class="info-row">
    <div class="chip">📍 <b>Champlain St-Lawrence</b></div>
    <div class="chip">🏋️ <b>3x</b>/week</div>
    <div class="chip">📅 <b>Mon, Wed, Thu</b> — 3–4PM*</div>
    <div class="chip">💪 Coach <b>Ash</b></div>
  </div>
  <p class="time-note">*Time window may extend — final schedule confirmed in the group chat (gym shared with volleyball 5–6PM Mon/Wed)</p>

  <form id="signupForm">
    <div>
      <label class="field-label">Full Name</label>
      <input type="text" id="fullName" placeholder="e.g. Alex Bédard" required>
    </div>

    <div>
      <label class="field-label">Student Number</label>
      <input type="text" id="studentNumber" placeholder="e.g. 1234567" required inputmode="numeric">
      <div class="field-hint">Required — no valid student number, no spot. No exceptions.</div>
    </div>

    <fieldset>
      <label class="field-label">Where do you fit right now?</label>
      <div class="radio-grid status">
        <label class="radio-card">
          <input type="radio" name="status" value="Returning Vet" required>
          <div class="title">Returning Vet</div>
          <div class="desc">Played with the Lions before, coming back for the season</div>
        </label>
        <label class="radio-card">
          <input type="radio" name="status" value="Current Core">
          <div class="title">Current Core</div>
          <div class="desc">Part of the summer program, continuing on</div>
        </label>
        <label class="radio-card">
          <input type="radio" name="status" value="Trying Out">
          <div class="title">Trying Out</div>
          <div class="desc">New to the program, want to earn a spot</div>
        </label>
      </div>
    </fieldset>

    <div>
      <label class="field-label">Position</label>
      <select id="position" required>
        <option value="" disabled selected>Select position</option>
        <option>Guard</option>
        <option>Wing</option>
        <option>Forward</option>
        <option>Center</option>
        <option>Not sure yet</option>
      </select>
    </div>

    <div>
      <label class="field-label">Instagram Handle</label>
      <input type="text" id="contact" placeholder="@yourhandle" required pattern="^@?[A-Za-z0-9._]+$" title="Enter a valid Instagram handle">
    </div>

    <label class="commit-toggle">
      <input type="checkbox" id="commit" required>
      <div>
        <div class="title">I can commit to all 3 sessions a week</div>
        <div class="desc">Consistency is what preseason is about — be honest here</div>
      </div>
    </label>

    <div>
      <label class="field-label">Anything we should know? (optional)</label>
      <textarea id="notes" placeholder="Conflicts, injuries, questions..."></textarea>
    </div>

    <button type="submit" class="submit" id="submitBtn">Confirm I'm In</button>
    <div class="error-msg" id="errorMsg">Something went wrong — try again or text Senam directly.</div>
    <p class="footer-note">Once you're in, we'll create an Instagram group and share all the details there.<br>The climb starts here 🦁</p>
  </form>

  <div class="success" id="successMsg">
    <div class="mark">
      <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
    </div>
    <h2>You're In</h2>
    <p>We'll create an Instagram group chat and share all the info there — dates, times, everything.<br>Get your rest, hydrate, and get ready to work.</p>
  </div>
</div>

<script>
  // Replace with your deployed Google Apps Script Web App URL.
  // See setup instructions in the accompanying README.
  const SCRIPT_URL = "https://script.google.com/macros/s/AKfycbyMX1TXI6Donuis5R-jaKknq_OEvfgK3Xm1LfDGB9wOylcIGz2fpdymf7NiSci24rUG/exec";

  const form = document.getElementById('signupForm');
  const btn = document.getElementById('submitBtn');
  const errorMsg = document.getElementById('errorMsg');

  form.addEventListener('submit', async function(e) {
    e.preventDefault();
    errorMsg.classList.remove('show');
    btn.disabled = true;
    btn.textContent = 'Submitting...';

    const data = {
      fullName: document.getElementById('fullName').value,
      studentNumber: document.getElementById('studentNumber').value,
      status: form.querySelector('input[name="status"]:checked')?.value || '',
      position: document.getElementById('position').value,
      contact: document.getElementById('contact').value,
      commit: document.getElementById('commit').checked ? 'Yes' : 'No',
      notes: document.getElementById('notes').value,
      timestamp: new Date().toISOString()
    };

    try {
      await fetch(SCRIPT_URL, {
        method: 'POST',
        mode: 'no-cors',
        headers: { 'Content-Type': 'text/plain' },
        body: JSON.stringify(data)
      });
      form.classList.add('hide');
      document.getElementById('successMsg').classList.add('show');
    } catch (err) {
      errorMsg.classList.add('show');
      btn.disabled = false;
      btn.textContent = "Confirm I'm In";
    }
  });
</script>

</body>
</html>
